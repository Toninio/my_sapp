
<%= simple_form_for(@player) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :team %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
